{
  "_comment": "Copy to secrets.json (gitignored). Values here override config.json. monthly_budget_usd is only a FALLBACK — the daemon fetches the official spend/limit from Anthropic's OAuth usage endpoint when ~/.claude/.credentials.json has a valid token.",
  "ble_device_name": "ClaudeObserver",
  "monthly_budget_usd": 500
}
